[Unit]
Description = SHM (Shared Memory) listener for ss963 serial driver board and SPI driver
After = multi-user.target

[Service]

User=root
SyslogIdentifier = ss963
StandardOutput = syslog+console
StandardError = syslog+console
EnvironmentFile = /etc/ss963.service.conf
ExecStart = /usr/sbin/ss963 --port-count=${PORT_COUNT} --latch-pin=${LATCH_PIN} --latch-delay=${LATCH_DELAY} --speed=${SPEED} --loop-delay-us=${LOOP_DELAY_US}
Type=idle


[Install]

WantedBy = multi-user.target

